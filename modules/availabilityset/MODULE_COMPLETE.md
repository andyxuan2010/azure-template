# Availability Set Module Complete

The `availabilityset` module includes:

- Terraform implementation files.
- Input validation and local naming/tag composition.
- README, examples, quick reference, and validation notes.
- Mocked plan tests under `tests/live.tftest.hcl`.
- Root harness wiring through `features.enable_availabilityset`, `module_plan_enabled.availabilityset`, and the `availabilitysets` sample map.
