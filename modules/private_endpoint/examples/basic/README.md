# Basic Private Endpoint

Creates a Private Endpoint from direct subnet, target resource, and Private DNS zone IDs.

Direct IDs are recommended when dependencies are produced by the same Terraform composition.
