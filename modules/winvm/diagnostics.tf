// Diagnostic settings removed from module due to provider schema compatibility
// differences. To enable VM diagnostics, add a provider-compatible
// `azurerm_monitor_diagnostic_setting` resource and set the module variables
// `enable_diagnostics` and `log_analytics_workspace_id` appropriately.
